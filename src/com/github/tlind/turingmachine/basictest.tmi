print("Starting turing machine...");

setValues(# 2 0);
setPosition(0);

generate();

print();
setCommand(
    AWARENESS=2, PAGE=0,
        ?setTape(3)
        ?move(1)
        ?goToPage(1)
        ?print()
        ?stop()
);

run();
print();
print("Finished.");
