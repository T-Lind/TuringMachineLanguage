print("Starting turing machine...");

setValues(# 2 0);
setPosition(0);

//const setAmount = 3;

setCommand(
    AWARENESS=2, PAGE=0,
        ?setTape(setAmount)
        ?awareness()
        ?stop()

);

run();
print("Finished.");
print();
